# Core data + ML stack for ViolationProto (Theme 1)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
requests>=2.28          # HTTP utility

# Gradient boosting challengers (HistGradientBoosting from sklearn is the
# dependency-free fallback; these are used if importable)
xgboost>=2.0
lightgbm>=4.0

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Web app backend (ML/data service) -- see webapp/
# The dashboard is now a React + Node/Express + FastAPI stack (webapp/), with
# Mappls maps. Node/Express handles the browser + Mappls token; FastAPI serves
# the model and pipeline outputs.
fastapi>=0.110
uvicorn>=0.29

# Optional: the deep-learning experiments (experiments/deep_models.py,
# nn_preprocessing.py, ...) need PyTorch. Not required for the pipeline or app.
# torch>=2.0
