# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*
!.env.example
.ipynb_checkpoints/

# --- Node / web ---
node_modules/
webapp/*/node_modules/
webapp/frontend/dist/
npm-debug.log*
.vite/

# --- Intermediate pipeline cache (regenerable by pipeline.py) ---
data/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: outputs/ and artifacts/ ARE committed so the web app is demoable from a
# fresh clone (the raw source CSV lives outside the repo and is not published).
repomix-output.xml

# Experiment result tables written by experiments/*.py — regenerable, and the
# findings are summarised in docs/MODELS.md / docs/FEATURE_ENGINEERING.md.
artifacts/feature_ablation.csv
artifacts/improve_all.csv
artifacts/nn_preprocessing.csv
artifacts/q90_encoder.csv
artifacts/deep_vs_tree*
artifacts/stacking_seed*.json
artifacts/blend_fulltrain_seed*.json
